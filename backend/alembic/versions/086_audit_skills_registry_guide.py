"""Explain audit methodologies and immutable multi-source atom registries."""

from alembic import op
import sqlalchemy as sa


revision = "086_audit_skills_registry_guide"
down_revision = "085_audit_atom_provenance"
branch_labels = None
depends_on = None

ARTICLE = {
    "id": "7010bd56-13a5-49af-a284-2dc0d5774cad",
    "slug": "audit-metodiki-i-istochniki-atomov",
    "title": "Аудит: методики и происхождение атомов",
    "summary": "Установка навыков, выбор модели и методики, ручные и исторические реестры, версии ТЗ и сравнение предложений.",
    "body": """## Установка методики

Администратор открывает «Интеграции», выбирает JSON или .skill и устанавливает версию. Пакет с SKILL.md и текстовыми справочниками читается как методика: DPMS не запускает его код и не скачивает внешние ссылки. Все поддерживаемые локальные инструкции сохраняются вместе с SHA-256 исходного архива. При отсутствии номера версии используется метка содержимого.

Исполняемый audit-tz остается отдельным типом: для него нужны проверенный hash и self-test в worker. Архив с произвольными скриптами не становится безопасной методикой от одного переименования. Методика не получает права изменять сервер, ставить статусы приемки или обходить подтверждение передачи данных провайдеру.

Разные методики могут быть доступны одновременно. Новая версия одной методики не переписывает старые результаты. Замена исполняемого движка декларативными правилами под тем же именем не выполняется молча.

Декларативный skill задает правила выделения требований в проверяемом формате DPMS. Команды запуска дополнительных агентов, программ и формирования произвольных файлов из текста skill не исполняются. Установка пакета не означает прохождения всех внешних процедур контроля качества, которые мог описать его автор.

## Обработка договора

В карточке договора выберите исходное ТЗ, методику и ИИ-подключение. Подготовка фиксирует конкретный документ и его hash. Перед внешним запросом подтвердите выбранное подключение; изменение документа, методики или конфигурации требует нового подтверждения. ИИ-обработка идет пакетами с сохранением уже выполненных частей.

Декларативные методики работают с DOCX и текстовыми PDF; исполняемый audit-tz работает с DOCX. Изображения и сканы без текстового слоя не распознаются. Извлечение текста и проверка структуры результата не доказывают полноту требований: реестр проверяет аудитор.

Результат каждого сочетания документа, версии методики и конфигурации ИИ сохраняется самостоятельным модельным реестром. В нем видны подключение, модель, версия конфигурации, методика, версия/hash методики и исходного ТЗ. Одинаковое имя файла не доказывает одинаковую версию документа.

## Генеральный реестр

В колонке «Источник» различаются историческая загрузка, ручной реестр, ручное создание и ИИ. Для ИИ указаны подключение/модель и методика; для импортов сохраняется связь с исходной загрузкой и строкой. Недостающие сведения старых записей не восстанавливаются догадкой: происхождение может быть обозначено как неизвестное. Исходные исторические книги остаются доступны только администраторам.

Новые ИИ-предложения на этапе атомизации добавляются как черновики. Они не становятся автоматически принятыми и не получают положительную альфа-проверку или решение комиссии. Повтор фиксации одного модельного результата не создает копий и не меняет уже отредактированные атомы.

Если договор уже перешел к проверкам или комиссии, модельный результат сохраняется отдельно. Для расширения генерального реестра руководитель должен явно вернуть договор на этап атомизации. Старые модельные результаты можно добавить отдельной командой; обновление системы само их не публикует.

Одинаковые формулировки из разных источников сохраняются как отдельные предложения с собственным происхождением. Число строк не является доказанным числом уникальных требований. Перед приемкой просмотрите предложения, уточните их и исключите подтвержденные дубли. Новая версия документа не переносит автоматически положительные результаты проверки старой версии.

## Сравнение

Сравнивать можно результаты разных подключений и методик по одной неизменяемой версии ТЗ. Результаты разных документов или hashes нельзя незаметно смешать как один анализ. Номера фрагментов локальны для метода извлечения и сами по себе не доказывают совпадение требований.

Сравнение показывает предложения и текстовые основания. Различающиеся формулировки, условия и отрицания не объявляются автоматическим согласием моделей. Сохранение решений сравнения не меняет исходные атомы, результаты альфа-проверки, комиссию или этап договора. Проверяющий отдельно принимает решения в генеральном реестре.

## Ручные и исторические данные

Обычный XLSX-реестр и исторический перенос остаются разными путями. Исторический импорт требует сопоставления, preview и подтверждения; даты и авторы сохраняются как исторические сведения. Новый ИИ-реестр не заменяет такую загрузку. Защищенная отмена исторического переноса остается доступна только пока его данные не изменены и не используются последующей работой.

Административный экспорт генерального реестра сохраняет происхождение и текстовые основания. Внесение новых атомов не является повторной приемкой ранее выполненных проверок.
""",
}


def upgrade() -> None:
    op.get_bind().execute(sa.text("""
        INSERT INTO knowledge_articles
            (id, slug, title, summary, section, body, status, sort_order,
             created_at, updated_at, published_at)
        VALUES (CAST(:id AS uuid), :slug, :title, :summary, 'audit', :body,
                'published', 84, now(), now(), now())
        ON CONFLICT (slug) DO UPDATE SET title=EXCLUDED.title,
            summary=EXCLUDED.summary, body=EXCLUDED.body, updated_at=now()
        WHERE knowledge_articles.id=EXCLUDED.id
    """), ARTICLE)


def downgrade() -> None:
    op.get_bind().execute(sa.text(
        "DELETE FROM knowledge_articles WHERE id=CAST(:id AS uuid) AND slug=:slug"
    ), ARTICLE)
