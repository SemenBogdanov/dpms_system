"""Document atomic AI provider verification.

Revision ID: 063_ai_verify_save
Revises: 062_synology_connection_profiles
"""

from alembic import op
import sqlalchemy as sa


revision = "063_ai_verify_save"
down_revision = "062_synology_connection_profiles"
branch_labels = None
depends_on = None


ARTICLE_ID = "1fb214df-b6b3-41bb-b02b-b4fbe08ef17e"

NEW_SUMMARY = "Как безопасно проверить, сохранить и активировать OpenAI-compatible API в DPMS."
NEW_BODY = """Обновлено: 2026-08-23

## Для чего нужен коннектор
DPMS использует один активный серверный профиль ИИ-провайдера. Бизнес-функции обращаются к нему через backend: API key не передается в браузер и не дублируется по разделам.

## Кто управляет подключением
Создавать, изменять, проверять, включать и отключать профиль может только системный администратор.

## Проверка и сохранение
1. Откройте `Админ -> Интеграции -> ИИ`.
2. Укажите название, HTTPS API URL, точный идентификатор модели и API key.
3. Выберите, должно ли подключение быть активным.
4. Нажмите `Проверить и сохранить`.

DPMS сначала выполняет короткий технический запрос к выбранной модели. Только после успешного ответа профиль сохраняется со статусом `OK`. Ошибка URL, модели, ключа, тарифа или доступности провайдера не заменяет ранее рабочую конфигурацию.

При последующем изменении настроек поле API key можно оставить пустым: сохраненный зашифрованный ключ останется прежним. Кнопка `Повторить проверку` проверяет уже сохраненный профиль.

## Преднастройка ROX-1
Несекретные значения подключения хранятся в `deploy/integrations.defaults`: название `ROX-1`, API URL `https://api.rox.one/v1` и модель `cx/gpt-5.6-sol-max`. В allowlist указывается только origin `https://api.rox.one`, без пути `/v1`.

API key в этот файл не добавляется. Администратор вводит его в интерфейсе, после чего DPMS проверяет провайдера и сохраняет ключ зашифрованно. Дополнительные разрешенные origins перечисляются через запятую в `AI_PROVIDER_ALLOWED_ORIGINS`.

## Статусы
- `OK · активно` — профиль проверен и может использоваться функциями системы.
- `OK · отключено` — настройки проверены, но обращения к модели запрещены.
- `Ошибка проверки` — сохраненный профиль не прошел последнюю повторную проверку.

Статус проверки привязан к конкретной версии настроек. Измененная или устаревшая версия не используется бизнес-функциями до успешной проверки.

## Безопасность
API key хранится только в зашифрованном виде и никогда не возвращается frontend. DPMS соединяется только с HTTPS-origin из серверного allowlist, не следует redirects и не записывает API key, prompts или ответы модели в журнал интеграции.

Наличие профиля не открывает пользователям произвольный чат. Каждая ИИ-функция получает отдельный бизнес-сценарий, ограничения входных данных, проверку результата человеком и ручной режим на случай недоступности модели.
"""

OLD_SUMMARY = "Как подключить OpenAI-compatible API и безопасно использовать его в функциях системы."
OLD_BODY = """Обновлено: 2026-08-22

## Для чего нужен коннектор
DPMS использует единый серверный профиль ИИ-провайдера. Конкретные функции системы обращаются к этому профилю через backend, поэтому API key не передается в браузер и не дублируется в разных разделах.

## Кто управляет подключением
Создавать, изменять, проверять и удалять профиль может только системный администратор.

## Настройка
1. Откройте `Админ -> Интеграции -> ИИ`.
2. Укажите название подключения, HTTPS API URL, точный идентификатор модели и API key.
3. Сохраните профиль.
4. Нажмите `Проверить подключение`. DPMS отправит короткий технический запрос выбранной модели.

При последующем изменении настроек поле API key можно оставить пустым: сохраненный ключ останется прежним.

## Безопасность
API key хранится только в зашифрованном виде и никогда не возвращается frontend. DPMS разрешает соединение только с HTTPS-origin из серверного allowlist, не следует redirects и не записывает prompts, ответы или ключ в журнал интеграции.

Доступ пользователей к каждой ИИ-функции задается самой бизнес-функцией. Наличие настроенного провайдера не открывает пользователям произвольный чат и не дает им доступ к API key.
"""


def _update_article(summary: str, body: str) -> None:
    op.get_bind().execute(
        sa.text(
            """
            UPDATE knowledge_articles
            SET summary = :summary,
                body = :body,
                updated_at = now(),
                published_at = COALESCE(published_at, now()),
                status = 'published'
            WHERE id = :id
            """
        ),
        {"id": ARTICLE_ID, "summary": summary, "body": body},
    )


def upgrade() -> None:
    op.add_column(
        "ai_provider_configs",
        sa.Column("last_verified_config_version", sa.Integer(), nullable=True),
    )
    op.create_check_constraint(
        "ck_ai_provider_configs_verified_version",
        "ai_provider_configs",
        "last_verified_config_version IS NULL OR last_verified_config_version >= 1",
    )
    _update_article(NEW_SUMMARY, NEW_BODY)


def downgrade() -> None:
    _update_article(OLD_SUMMARY, OLD_BODY)
    op.drop_constraint(
        "ck_ai_provider_configs_verified_version",
        "ai_provider_configs",
        type_="check",
    )
    op.drop_column("ai_provider_configs", "last_verified_config_version")
