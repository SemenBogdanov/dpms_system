"""Available meeting windows in the native calendar guide."""
from alembic import op
import sqlalchemy as sa

revision = "093_calendar_windows_guide"
down_revision = "092_calendar_planning_guide"
branch_labels = None
depends_on = None

ADDITION = """

## Доступные окна на графике встреч

Строка «Доступные окна» помогает подобрать время для группы и докладчика. Выберите длительность (по умолчанию 30 минут), при необходимости группу и конкретного докладчика. Подбор доступен за период до 31 дня. Он учитывает полную длительность, состав группы на выбранную дату, доступность аудитора, техспециалиста и докладчика, отсутствие и все их бронирования, даже скрытые фильтром графика.

Зеленое окно означает хотя бы один вариант, в котором все трое подтвердили свободное время. Светло-желтое означает, что подтвержденного варианта нет, но есть вариант с неуказанной доступностью. Это предупреждение V5, а не подтверждение участия. Явная занятость, отпуск или свободный интервал короче встречи не считаются подходящим окном. Нейтральное окно означает отсутствие вариантов. Прошедшее время не предлагается.

Число в окне — количество сочетаний группы и докладчика, а не число независимых встреч: один сотрудник может входить в несколько вариантов. Нажмите окно, проверьте конкретный состав и именные предупреждения. Выбор варианта открывает обычную форму новой встречи с заполненными датой, временем, длительностью, группой и докладчиком. Встреча появится только после явного сохранения помощником; открытие окна ничего не бронирует.

При открытии вариантов сервер проверяет расписание заново. При сохранении встречи выполняется окончательная проверка; если другой помощник успел занять время, повторного бронирования не будет. При ошибке загрузки старые цвета не подтверждают доступность. Обновите график и повторите подбор.

Обычный график подбирает интервалы целиком в окне 10:00–18:00. Когда график показывает полный день из-за записей за пределами этого окна, подбор проверяет весь день, без перехода через полночь. Доступность конкретного выходного определяется внесенными интервалами и предупреждениями V5, как и при обычном создании встречи. Для слишком большого числа сочетаний выберите конкретную группу или докладчика.
"""


def upgrade():
    op.execute("SET LOCAL lock_timeout = '5s'")
    op.get_bind().execute(sa.text("""
        UPDATE knowledge_articles SET body=body || :addition, updated_at=now()
        WHERE id=CAST(:id AS uuid) AND slug='kalendar-audita-plan-fakt'
    """), {"id": "f417f29b-c75c-40e7-9cb0-6d5bfa39fcd6", "addition": ADDITION})


def downgrade():
    raise RuntimeError("Use a reviewed forward migration to change the published calendar guide.")
