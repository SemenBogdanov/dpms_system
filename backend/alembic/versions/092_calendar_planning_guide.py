"""Concurrent availability and calendar workload guide."""
from alembic import op
import sqlalchemy as sa

revision = "092_calendar_planning_guide"
down_revision = "091_calendar_request_guards"
branch_labels = None
depends_on = None

ADDITION = """

## Независимый доступ к календарю

Допуск к «Календарю аудита» не зависит от допуска к разделу «Аудит», глобальной очереди или модуля развития. Администратор отдельно включает допуск к календарю и активное участие в его контуре. Право помощника назначается отдельно. Доступ только к «Аудиту» сам по себе не открывает календарь.

## Одновременное заполнение интервалов

Сотрудники могут заполнять разные даты и интервалы одновременно. Изменение чужого расписания или нормы больше не вызывает конфликт версии при сохранении доступности. Сервер сравнивает только исходные значения затронутых получасовых интервалов и проверяет актуальные права, отсутствие, замок и архив.

Если тот же интервал уже изменен иначе, черновик сохраняется. Нажмите обновление для сравнения, проверьте исходное, текущее и предлагаемое значения, затем явно подтвердите применение. Молчаливой перезаписи чужого результата нет. Совпадающее итоговое значение не является конфликтом. Промежуточные изменения остаются в истории даже если значение вернулось к исходному.

При потере связи повторяется тот же запрос: он не создает второе изменение. Если запись уже сохранена, а обновить экран не удалось, система предлагает только перечитать актуальное состояние. Сохранение данных и получение нового отображения различаются.

## Нормы групп и выбор времени встречи

Помощник изменяет норму прямо из строки группы в «Управлении» или «Справочниках». Норма группы задается на 10 рабочих дней. Новая редакция действует с выбранной даты, требует основания и не переписывает прошлые нормы. Нулевая норма допустима; общая дневная норма команды и нормы групп независимы.

В новой встрече список групп проверяется для выбранных даты, начала и полной длительности. Учитываются действующий состав, занятость, отсутствие и другие встречи участников, а после выбора докладчика также его доступность. Если дата или длительность меняются, варианты обновляются. Недоступная уже выбранная группа не заменяется молча. Сохранение неполного черновика не равнозначно назначению встречи.

Неуказанная доступность по правилам V5 дает предупреждение, но сама по себе не запрещает встречу. Явная занятость и отсутствие блокируют назначение. В предупреждении указан код конкретного участника; это источник замечания, а не оценка его ответственности.

## Отчётность: загрузка свободного времени

За выбранный период показаны сотрудник, заполненные рабочие дни, количество плановых встреч, внесенные свободные слоты и загрузка. Один слот равен 30 минутам. Полностью заполненный день означает известное состояние всех интервалов, а не полную свободу; частичное заполнение и отсутствие различаются.

Загрузка (%power) = минуты плановых встреч в рабочем окне / минуты внесенного свободного времени в том же окне × 100%. Рабочее окно — Пн–Пт, 10:00–18:00, Europe/Moscow. Например, 90 минут встреч при 60 минутах доступности дают 150%. Если свободного времени нет или оно не указано, процент не рассчитывается, а не подменяется нулём.

Количество встреч и их полная длительность показаны отдельно; встреча на 90 минут — одна встреча, а не три. Время за пределами рабочего окна вынесено отдельно. Отмененные планы и встречи с отмененным фактом исключаются; проведенная встреча не учитывается второй раз поверх своего плана. При фильтре группы учитываются ее встречи, а доступность остается общей личной доступностью сотрудника. Такой процент показывает долю времени этой группы, а не общую нагрузку человека.

Это показатель планирования, не доказательство производительности или фактического участия. Сравнение плана с нормой приводится отдельно в деталях и не подменяет загрузку относительно свободного времени.
"""


def upgrade():
    op.execute("SET LOCAL lock_timeout = '5s'")
    op.get_bind().execute(sa.text("""
        UPDATE knowledge_articles SET body=body || :addition, updated_at=now()
        WHERE id=CAST(:id AS uuid) AND slug='kalendar-audita-plan-fakt'
    """), {"id": "f417f29b-c75c-40e7-9cb0-6d5bfa39fcd6", "addition": ADDITION})


def downgrade():
    raise RuntimeError("Use a reviewed forward migration to change the published calendar guide.")
