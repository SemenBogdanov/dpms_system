# Графы: интеграция принятого локального слайса

## Решение пользователя

25.09.2026 пользователь сравнил localhost:55177 с production и явно разрешил
выпустить именно улучшенную локальную версию. Постоянный тестовый адрес из
пункта1 не меняется. Исходный чек-лист сохранён в ACCEPTANCE-2026-09-24-ORIGINAL.md.

## Состав

- 2.1: явная галочка линзы и сохранение её состояния.
- 2.2: последовательный ввод стикеров через Tab на холсте.
- 2.3: полная справка мастера, ссылка в базу знаний и выбор статьи по slug.
- 2.4–2.6: двадцать портов, изменение размера, прилегание наконечника,
  предварительная стрелка и мягкая магнитность.
- 2.7–2.8: раскрытие списка по высоте, масштабирование колесом и инверсия.
- 2.12: пять режимов JSON-импорта, mapping, preview и явное подтверждение.
- 3.1: тема графа наследуется от DPMS без перезагрузки рабочего пространства.
- 3.2: дальнейшее выравнивание остаётся в бэклоге, новых изменений здесь нет.

## Граница релиза

База: production8457a3e. Исходники графа, его три модуля, оболочка GraphsPage,
KnowledgePage и schema graph побайтово совпадают с принятым локальным кандидатом.
Четыре workspace-ресурса также сверены с фактическим HTTP на55177.
SHA-256 index.html: `9b0ed5af190d74bb004f2a14a773de67995307475df146b31393bc3d3c99a921`.

Локальная data migration097 перенесена в новую production099 с родителем
098_server_boot_guide. Старые migration-файлы и release manager не меняются.
Boot recorder, worker, админка и статья о VPS остаются production-версиями.
Локальная БД и пользовательские графы не переносятся и не перезаписываются.

## Проверки интегратора

- Node:50 маршрутов +27 импортов PASS.
- Backend:13 storage/schema PASS на Python3.11 и3.14,14 migration PASS,
  8 boot-guide regression PASS.
- PostgreSQL: disposable schema; единственная вершина099, публикация и повтор,
  сохранение boot-статьи и ручной редакции при upgrade/downgrade PASS.
- Build, focused lint и shell syntax PASS.
- Первоначальная проверка storage в вспомогательном образе не имела aiosqlite;
  после установки test-only зависимости в удаляемый контейнер13/13 PASS.
  Production зависимости не менялись.
- Independent critic GO; условие совместимого forward-fix записано в INTEGRATION.md.
- Browser:99/99 Chromium,46PASS/53desktop-onlySKIP для каждого WebKit portrait
  и landscape,35/35 knowledge-code regression. Итого226PASS,106SKIP,0FAIL,
  0flaky. Ряд тестов использует собственный viewport; физический iPhone не
  проверялся. Скриншоты списка и мобильного JSON-импорта просмотрены.
- Production evidence фиксируется в release report PersonalOS после выкладки.

## Выпуск и восстановление

Перед promote обязателен проверенный backup. После: schema099, обе статьи
опубликованы, exact images/assets, API guards и health. Четыре публичных
workspace-ресурса production должны совпасть с принятым локальным кандидатом.

После новых сохранений обычный rollback на старый frontend/backend небезопасен:
теряются или отвергаются ports/lens. Использовать forward-fix с сохранением нового
контракта; не делать автоматический downgrade, restore БД или JSON-переписывание.
Подробности в INTEGRATION.md.
